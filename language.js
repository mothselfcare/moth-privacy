(()=>{document.querySelectorAll(".language-switch").forEach(link=>{link.addEventListener("click",()=>{try{localStorage.setItem("moth-language",link.dataset.language||"")}catch{}})});const menus=[...document.querySelectorAll(".mobile-site-menu")];if(!menus.length)return;const close=menu=>{if(menu.open)menu.open=false};document.addEventListener("click",event=>{menus.forEach(menu=>{if(!menu.contains(event.target))close(menu)})});document.addEventListener("keydown",event=>{if(event.key!=="Escape")return;menus.forEach(menu=>{if(!menu.open)return;close(menu);const summary=menu.querySelector("summary");if(summary)summary.focus()})});menus.forEach(menu=>{menu.querySelectorAll(".mobile-site-menu-panel a").forEach(link=>link.addEventListener("click",()=>close(menu)))})})();